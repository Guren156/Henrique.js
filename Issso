/* Reset básico e estilos gerais */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

body {
  font-family: Arial, sans-serif;
  background-color: #f4f4f4;
  color: #333;
  line-height: 1.6;
}

/* Container principal da aplicação */
.container {
  max-width: 1200px;
  margin: 20px auto;
  padding: 20px;
  background-color: #fff;
  border-radius: 8px;
  box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

/* Estilo do cabeçalho */
header {
  text-align: center;
  margin-bottom: 20px;
}

/* Estilo para todos os botões */
button {
  background-color: #007bff;
  color: white;
  border: none;
  padding: 10px 15px;
  border-radius: 5px;
  cursor: pointer;
  font-size: 16px;
  transition: background-color 0.3s ease;
  margin-right: 10px; /* Espaço entre botões */
}

button:hover {
  background-color: #0056b3;
}

/* Estilo da lista de produtos */
.product-list {
  display: grid;
  gap: 20px;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  margin-top: 20px;
}

/* Estilo de cada item de produto individual */
.product-item {
  border: 1px solid #ddd;
  padding: 20px;
  border-radius: 8px;
  background-color: #f9f9f9;
  display: flex;
  flex-direction: column;
}

.product-item h3 {
  margin-bottom: 5px;
}

.product-item p {
  margin-bottom: 10px;
}

.product-item button {
  align-self: flex-start; /* Alinha o botão à esquerda dentro do item */
  margin-top: auto; /* Empurra o botão para a parte inferior */
}

/* Estilo do formulário de adição de produto */
.product-form {
  display: flex;
  flex-direction: column;
  gap: 15px;
  margin-top: 20px;
  padding: 20px;
  border: 1px solid #ddd;
  border-radius: 8px;
}

.product-form label {
  font-weight: bold;
}

.product-form input[type="text"],
.product-form input[type="number"],
.product-form textarea {
  width: 100%;
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 5px;
  font-size: 16px;
  margin-top: 5px;
}

.product-form textarea {
  resize: vertical; /* Permite redimensionar apenas verticalmente */
}

/* Estilo do rodapé */
footer {
  margin-top: 40px;
  text-align: center;
  font-size: 14px;
  color: #666;
  padding-top: 20px;
  border-top: 1px solid #ddd;
}

/* Responsividade */
@media (max-width: 768px) {
  .container {
    margin: 10px;
    padding: 10px;
  }

  .product-list {
    grid-template-columns: 1fr; /* Em telas menores, mostra 1 produto por linha */
  }
  
  .product-form {
    padding: 15px;
  }
}
